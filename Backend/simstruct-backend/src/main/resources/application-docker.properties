# Docker Profile Configuration
# Activated when SPRING_PROFILES_ACTIVE=docker

# AI Model API Configuration (Docker)
ai.api.url=http://ai-model:8000

# Logging
logging.level.com.simstruct=DEBUG

# CORS (allow frontend container)
cors.allowed-origins=http://localhost:4200,http://frontend:80
