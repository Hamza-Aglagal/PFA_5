package com.simstruct.tests.pages;

import org.openqa.selenium.WebDriver;

/**
 * Page Object pour l'historique
 */
public class HistoryPage extends BasePage {
    public HistoryPage(WebDriver driver) {
        super(driver);
    }
}
