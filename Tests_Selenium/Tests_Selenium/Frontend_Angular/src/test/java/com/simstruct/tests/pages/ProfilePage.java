package com.simstruct.tests.pages;

import org.openqa.selenium.WebDriver;

/**
 * Page Object pour le profil
 */
public class ProfilePage extends BasePage {
    public ProfilePage(WebDriver driver) {
        super(driver);
    }
}
